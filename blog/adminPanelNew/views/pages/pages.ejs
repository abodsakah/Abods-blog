<%- include("../addons/head"), {title:title} %> 
<%- include("../addons/nav") %>

<div class="content-padder content-background">
    <div class="uk-section-small uk-section-default header">
        <div class="uk-container uk-container-large">
            <h1><span class="ion-speedometer"></span> Page list</h1>
            <p>
                Welcome back, <%= user[0].first_name %> <%= user[0].last_name %>!
            </p>
            <ul class="uk-breadcrumb">
                <li><a href="/">Home</a></li>
                <li><span href="">Page list</span></li>
            </ul>
        </div>
    </div>

    <div class="uk-section-small">
        <div class="uk-container uk-container-large">
            <a class="uk-button uk-button-primary" style="float: right;" href="/dashboard/create/page"> <span uk-icon="icon: plus"></span> Create page
            </a>
            <br><br>
            <div uk-grid class="uk-child-width-1-1@s uk-child-width-1-1@l">
                
                <div>
                    <div class="uk-card uk-card-default">
                        <div class="uk-card-header">
                            Website Table
                        </div>
                        
                        <div class="uk-card-body">
                            
                            <table class="uk-table uk-table-striped">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Title</th>
                                        <th>Status</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% for(var page of pages) { %>
                                    <tr>
                                        <td> <%= page.id %>  </td>
                                        <td> <%= page.title %>  </td>
                                        <% if(page.status === 1){ %> 
                                            <td> Published  </td>
                                        <% } else if(page.status === 2) { %>
                                            <td> Draft  </td>
                                        <% } else { %>
                                            <td> Private  </td>
                                        <% } %>
                                        <td> <a href="edit/page/<%= page.id %>"><span uk-icon="pencil"></span></a>
                                            <a href="delete/page/<%= page.id %>"><span uk-icon="trash"></span></a>
                                        </td>

                                    </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
</div>

<%- include("../addons/footer") %> 